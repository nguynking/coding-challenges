#include <bits/stdc++.h>
using namespace std;

int main() {
    int n; cin >> n;
    int x;
    vector<int> num(n + 1);
    vector<int> cnt(n + 1, 0);
    for (int i = 0; i < n; i++) {
        cin >> x;
        num.push_back(x);
    }
    int start = 0, end = 0;
    while (end < num.size()) {
        
    }
    return 0;
}